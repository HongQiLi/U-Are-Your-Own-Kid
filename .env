# 允许前端跨域（开发期先全开放）
# Allow all origins in dev 
ALLOWED_ORIGINS=*

# SQLite 数据库文件放在仓库根目录下的 app.db
# SQLite DB file at repo root
DATABASE_URL=sqlite+aiosqlite:///./app.db

# JWT 密钥
# JWT secret (use a stronger one in prod)
JWT_SECRET=

# OpenAI API Key
OPENAI_API_KEY=
